# =============================================================
# OS Hardening role — точка входа
# =============================================================

---
- name: Sysctl hardening
  include_tasks: "{{ role_path }}/tasks/sysctl.yml"

- name: Services hardening
  include_tasks: "{{ role_path }}/tasks/services.yml"

- name: Auditd setup
  include_tasks: "{{ role_path }}/tasks/auditd.yml"
